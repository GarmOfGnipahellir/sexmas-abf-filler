import * as React from 'react';
import { Button } from '@blueprintjs/core';
import { remote } from 'electron';

export class Personal extends React.Component<undefined, undefined> {
  render() {
    return (
      <div>
        <h1>Sheets</h1>
      </div>
    );
  }
}
